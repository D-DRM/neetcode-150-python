#1. 242. Valid Anagram
# ARRAYS AND HASHING
# DIFFICULTY: Easy
class Solution:
    def isAnagram(self, s: str, t: str) -> bool:
        from collections import Counter
        s1= Counter(s)
        t1= Counter(t)
        if s1==t1:
            return True
        else:
            return False

        